<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    android:layout_width="match_parent"
    android:layout_height="35dp"
    android:orientation="horizontal"
    android:background="@color/bgWhite"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <RelativeLayout
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_weight="1"
        android:gravity="start">

        <com.bcasekuritas.mybest.widget.textview.CustomTextView
            android:id="@+id/tv_orderbook_ask"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentStart="true"
            android:layout_centerInParent="true"
            android:paddingStart="10dp"
            android:text="0"
            app:txtFor="caption"
            app:txtStyle="softBold" />

        <ProgressBar
            android:id="@+id/progress_indicator_ask"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            style="?android:attr/progressBarStyleHorizontal"
            android:layout_marginBottom="2dp"
            android:layout_gravity="center"
            android:progressBackgroundTint="@color/bgWhite"
            android:progressTint="@color/textDown"
            android:indeterminate="false"
            android:progress="0"
            android:layoutDirection="ltr"
            android:layout_below="@id/tv_orderbook_ask"
            android:layout_alignParentBottom="true"/>
    </RelativeLayout>

    <com.bcasekuritas.mybest.widget.textview.CustomTextView
        android:id="@+id/tv_orderbook_lot_ask"
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_weight="1"
        android:gravity="end|center_vertical"
        android:paddingEnd="20dp"
        android:text="0"
        android:textColor="@color/textPrimary"
        app:txtFor="caption"
        app:txtStyle="softBold" />

</LinearLayout>
